<!DOCTYPE html>
<html>
  <head>
    <title>Dubate</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# gon %>
    <%= include_gon %>

    <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <div class="bg-slate-300">
        <nav>
          <ul class="md:flex">
            <% if user_signed_in? %>
              <li class="border-b md:border-none"><strong><%= link_to current_user.name, user_path(current_user), class: "block px-8 py-2   my-4 " %></strong></li>
              <li class="border-b md:border-none"><%= link_to 'プロフィール変更', edit_user_registration_path, class: "block px-8 py-2 my-4   " %></li>
              <li class="border-b md:border-none"><%= link_to '議論作成', new_debate_path, class: "block px-8 py-2 my-4 " %></li>
              <li class="border-b md:border-none"><%= link_to '議論一覧', debates_path, class: "block px-8 py-2 my-4 " %></li>
              <li class="border-b md:border-none"><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "block px-8  py-2 my-4 bg-black text-white font-bold py-2 px-4 rounded" %></li>
            <% else %>
              <li class="border-b md:border-none"><%= link_to 'サインアップ', new_user_registration_path, class: "block px-8 py-2 my-4  bg-black text-white font-bold py-2 px-4 rounded" %></li>
              <li class="border-b md:border-none"><%= link_to 'ログイン', new_user_session_path, class: "block px-8 py-2 my-4 bg-black  text-white font-bold py-2 px-4 rounded" %></li>
            <% end %>
          </ul>
        </nav>
      </div>
    </header>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
