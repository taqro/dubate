<td>
  <%= link_to debate.agenda, debate %>
</td>
<td>
  <%= debate.created_user_id %> <%= User.find(debate.created_user_id).name%>
</td>
<td>
  <% if !debate.joined_user_id.nil? %>
    <%= debate.joined_user_id %> <%= User.find(debate.joined_user_id).name %>
  <% end %>
</td>
<td>
  <% if debate.joined_user_id.nil? %>
    <p>議論開始前</p>
  <% else %>
    <% if debate.WinOrLose.nil? and debate.status == "before_vote" %>
      <p>議論中</p>
    <% end %>
    <% if debate.WinOrLose.nil? and debate.status == "voting" %>
      <p>投票中</p>
    <% end %>
    <% if !debate.WinOrLose.nil?%>
      <p>議論終了</p>
    <% end %>
  <% end %>
</td>
<td>
  <%= link_to '削除', debate, method: :delete, data: {confirm: "議論「#{debate.agenda}」を削除しますか？"}, class: 'btn btn-danger' %>
</td>
<td>
  <%= link_to '編集', edit_debate_path(debate), class: 'btn btn-danger' %>
</td>
<% if current_user.liked?(debate) %>
  <td>
    <%= button_to 'いいねを取り消す', debate_likes_path(debate), method: :delete, remote: true %>
  </td>
<% else %>
  <td>
    <%= button_to 'いいね', debate_likes_path(debate), remote: true %>
  </td>
<% end%>
<td>
  <%= debate.liked_users.count %>
</td>
<%# 何時間前に議論作成したか %>
<td>
</td>
